<%= form_for(current_user.favorite_connections.build, remote: true,url:favorite_user_path(user)) do |f| %>
  <%= hidden_field_tag :favored_id, user.id %>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>